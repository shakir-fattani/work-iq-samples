# For production, pin exact versions with hashes:
#   pip-compile --generate-hashes requirements.in -o requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
azure-identity>=1.25.3
pyjwt[crypto]>=2.9
